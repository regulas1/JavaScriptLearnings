class Node {

  constructor(data, next = null){
    this.head = head;
    this.next = next;
  
  }
//Add
add(value)
{
  const node = new Node(value)
  let current;

  if(!this.head)
}

//index of Value

// map

// mapValues

//clear

// from array

// remove from 


// get array

// get size





}